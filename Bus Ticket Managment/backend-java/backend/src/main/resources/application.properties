# Server Configuration
server.port=8080
server.servlet.context-path=/api/v1

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bus_ticket_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration   
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=e305967294d6a0af7f15eb759aa5886a3119dfee0631395916c38dccc0e493a1b643121c22f1ee034b2c1967d358e75283c391ee6990eb11b004444df6b7ac2c
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Email Configuration (for notifications)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.busticket=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:3001}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui
springdoc.swagger-ui.operationsSorter=method

# Application Configuration
app.name=Bus Ticket Reservation System
app.version=1.0.0
app.description=Comprehensive bus ticket reservation management system

# Allow circular references (temporary fix for dependency cycle)
spring.main.allow-circular-references=true

# Seat Hold Configuration
app.seat-hold-timeout=300000
app.max-seats-per-booking=10

# Enforce UTF-8 encoding for requests/responses
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# Payment Configuration
app.payment.gateway=stripe
app.payment.currency=USD

# QR Code Configuration
app.qr-code.width=200
app.qr-code.height=200
app.qr-code.format=PNG

# Admin Invite Code (empty disables self-admin registration)
app.admin.invite-code=0452
